Here is everything in one block — copy and paste it all:

---

You are an expert senior frontend developer. Build a complete, professional, fully working Amman Jordan based Football Court Booking Web Application using only HTML, CSS, and JavaScript. No frameworks. No React. No TypeScript. No build tools. Separate files for HTML, CSS, and JS. I will build the backend myself in ASP.NET Core with JWT authentication. Every feature must be fully implemented, no placeholders, no truncation. The project structure is: index.html for browsing courts, court.html for court details and slot booking, matches.html for open matches and joining games, dashboard.html for the court owner panel, login.html for login and register, settings.json for all API URLs, css/main.css for global styles, css/court.css for court details styles, css/dashboard.css for dashboard styles, js/settings.js for loading settings globally, js/auth.js for JWT handling and login modal and auth guard, js/api.js for all API calls, js/index.js for browse courts logic, js/court.js for court details logic, js/matches.js for open matches logic, js/dashboard.js for owner dashboard logic. Create settings.json with this exact content: apiBaseUrl is http://localhost:5000/api and endpoints are login at /auth/login, register at /auth/register, refreshToken at /auth/refresh, getCourts at /courts, getCourtById at /courts/{id}, searchCourts at /courts/search, getSlots at /courts/{id}/slots, bookSlot at /bookings, getMyBookings at /bookings/my, cancelBooking at /bookings/{id}/cancel, getOpenMatches at /matches, joinMatch at /matches/{id}/join, createOpenMatch at /matches, getOwnerCourt at /owner/court, createCourt at /owner/court, updateCourt at /owner/court, getOwnerSlots at /owner/slots, createSlot at /owner/slots, updateSlot at /owner/slots/{id}, deleteSlot at /owner/slots/{id}, getOwnerBookings at /owner/bookings, confirmBooking at /owner/bookings/{id}/confirm, cancelOwnerBooking at /owner/bookings/{id}/cancel, getSports at /sports, getAmenities at /amenities, defaultProject is Initiatives. Load settings on every page using settings.js which fetches settings.json then checks localStorage for overrides under key app_settings_override and merges them, and exposes a buildUrl function that takes an endpoint key and a params object and replaces {id} style placeholders in the URL. Use this mock data when API is unavailable: four courts named ملعب النجوم in خلدا rated 4.8 with 24 reviews indoor artificial turf pricing 25 JOD for 1 hour 35 JOD for 90 minutes 40 JOD for 2 hours with open match at 8 PM needing 3 players, ملعب الأبطال in دابوق rated 4.5 with 18 reviews outdoor natural grass pricing 20 JOD for 1 hour 30 JOD for 90 minutes 38 JOD for 2 hours no open match, ملعب الوحدات in الوحدات rated 4.2 with 31 reviews indoor parquet pricing 18 JOD for 1 hour 28 JOD for 90 minutes 35 JOD for 2 hours with open match at 6 PM needing 5 players, ملعب الشميساني in الشميساني rated 4.6 with 42 reviews outdoor artificial turf pricing 22 JOD for 1 hour 32 JOD for 90 minutes 42 JOD for 2 hours no open match. Use Unsplash football court images for all mock courts. Mock slots are ten slots at times 08:00 09:30 11:00 12:30 14:00 15:30 17:00 18:30 20:00 21:30 with statuses available booked available open_match available booked available open_match available booked. The backend API contract I will implement in ASP.NET Core is: POST /api/auth/login with body email and password returning token refreshToken and user object with id name email and role. POST /api/auth/register with body name email password phone and role as customer or owner. POST /api/auth/refresh with body refreshToken returning new token. GET /api/courts with query params sport area and search returning Court array. GET /api/courts/{id} returning single Court. GET /api/courts/search with query params q sport area returning Court array. GET /api/courts/{id}/slots with query param date as YYYY-MM-DD returning Slot array. POST /api/bookings with body courtId slotId date duration and openForJoin boolean returning Booking. GET /api/bookings/my with Bearer token returning Booking array. DELETE /api/bookings/{id}/cancel with Bearer token. GET /api/matches with query params date and area returning OpenMatch array. POST /api/matches/{id}/join with Bearer token. POST /api/matches with Bearer token and body courtId slotId date playersNeeded. GET /api/owner/court with Bearer token owner role returning Court. POST /api/owner/court with Bearer token and Court body. PUT /api/owner/court with Bearer token and Court body. GET /api/owner/slots with Bearer token and optional date query returning Slot array. POST /api/owner/slots with Bearer token and body time date isRecurring recurringDays array. PUT /api/owner/slots/{id} with Bearer token and Slot body. DELETE /api/owner/slots/{id} with Bearer token. GET /api/owner/bookings with Bearer token returning Booking array. PUT /api/owner/bookings/{id}/confirm with Bearer token. PUT /api/owner/bookings/{id}/cancel with Bearer token. GET /api/sports returning Sport array. GET /api/amenities returning Amenity array. All responses JSON all requests Content-Type application/json handle all HTTP errors with toast messages. The auth system in auth.js stores JWT in localStorage under ملعبي_token stores user object under ملعبي_user and refresh token under ملعبي_refresh. Every protected API call includes Authorization Bearer token header. If any API returns 401 try refresh once and if refresh fails call logout which clears all localStorage keys and redirects to index.html. Auth guard function checks if user is logged in and if not opens the login modal instead of proceeding. Login modal has two tabs Login and Register. After successful login close modal and retry the original action. After successful register redirect based on role. For index.html build the browse courts page with a fixed top navbar 64px tall containing the logo and app name ملعبي on the right side since it is RTL with English subtitle Courts App, sport selector tabs with Football active and Padel and Basketball as coming soon greyed out, a quick filter toggle button مباريات ناقصها لاعبين that filters courts showing only those with open matches, an area filter dropdown, a search input with real time filtering as user types, an أضف ملعبك button that opens login modal if guest or goes to dashboard.html if owner, and a login button that if logged in shows user avatar and name with dropdown for My Bookings and Logout and if guest shows Login text. Hero section with bold Arabic headline احجز ملعبك في عمّان and subtitle أفضل ملاعب كرة القدم في عمّان بين إيدك and a stats bar showing total courts count, areas covered count, and open matches today count. Courts grid is responsive 1 column on mobile 2 on tablet 3 on desktop 4 on wide screens. Each court card shows primary image with Indoor or Outdoor badge and surface type tag overlaid, court name bold, area and city with location pin icon, star rating and review count, pricing tiers list showing all options like 1h: 25 JOD and 1.5h: 35 JOD and 2h: 40 JOD, a pulsing blue open match badge showing مطلوب X لاعبين اليوم at TIME if hasOpenMatch is true, and two buttons عرض التفاصيل linking to court.html?id= and حجز سريع with auth guard. Show loading skeleton cards while fetching and empty state message if no courts match filters. Quick booking modal opens when Quick Book is clicked and user is logged in showing court name pricing tiers date picker available time slots as pills duration selector open match toggle and confirm button calling POST /api/bookings with success showing booking reference number. For court.html read court id from URL query param and fetch from GET /api/courts/{id}. Show image gallery with large primary image and thumbnail strip and lightbox on click. Court info section with name area city type badge surface type star rating review count and amenities grid where each amenity shows as icon and label pill using car icon for parking coffee for cafe sun for lighting door for changing rooms and droplets for showers greyed out if court does not have it. Pricing table with Duration Price and Book Now columns and a الأكثر طلباً badge on the most popular tier. Date selector is a horizontal scrollable strip of next 7 days with today highlighted and on date change fetch slots from GET /api/courts/{id}/slots?date=. Slot grid shows green buttons for available slots red disabled buttons with strikethrough for booked slots and blue pulsing buttons for open match slots. Clicking available slot opens booking modal with auth guard. Clicking open match slot shows join match modal. Booking modal has court pre-selected with duration selector based on pricing tiers open match toggle and confirm button calling POST /api/bookings with success confirmation and booking number. For matches.html show the same navbar with page title مباريات ناقصها لاعبين and filters for date as today tomorrow this week, area dropdown, and players needed filter as 1-2 or 3-4 or 5 plus. Each match card shows court name and area date and time players needed as متبقي X أماكن with person icons and انضم للمباراة button with auth guard. Show empty state message ما في مباريات متاحة هلأ — احجز ملعب وافتح مباراة when no matches. For dashboard.html apply full auth guard on page load redirecting to login.html if not logged in or to index.html with toast هاي الصفحة للملاعب بس if logged in but not owner role. Layout is a fixed 240px wide sidebar on the right since RTL with logo nav items نظرة عامة and الملعب and المواعيد and الحجوزات and الإعدادات and logout button at bottom, and main content area taking remaining width. Overview tab shows stats cards for today bookings count, this week revenue in JOD, total bookings this month, open matches count, a recent bookings table with customer name date time duration amount status badge and action buttons, and quick action buttons for Add Slot and View All Bookings. My Court tab has a form to create or update court with fields court name in Arabic, area dropdown using the real Amman areas listed below, court type indoor or outdoor, surface type, description textarea, amenities multi-select checkboxes, pricing tiers with add and remove rows for duration and price, and image URL input with preview. Save button calls PUT /api/owner/court and Preview button opens court.html in new tab. Slots tab has a date selector, weekly calendar view showing slots per day as colored blocks, add slot form with time picker duration price override and recurring toggle for selecting days of week, and each existing slot shows time status booking info if booked and edit and delete buttons, with bulk actions to mark day as closed or copy slots from another day. Bookings tab has a table with customer name and phone court date time duration amount and status, filter tabs for All Pending Confirmed Cancelled, confirm button on pending calling PUT /api/owner/bookings/{id}/confirm, cancel button with confirmation modal calling PUT /api/owner/bookings/{id}/cancel, and Export CSV button. Settings tab has API base URL input all endpoint inputs pre-filled from settings.json save to localStorage button reset to default button test connection button calling GET /api/sports and showing green Connected or red Cannot connect badge and clear all drafts button. For login.html build a clean centered card with two tabs. Login tab has email input password input with show hide toggle remember me checkbox and login button calling POST /api/auth/login. On success save token and user to localStorage and redirect owner to dashboard.html and customer to index.html or the page they came from using a returnUrl query param. Register tab has full name input email input phone input for Jordan format 07X-XXXX-XXX password input with strength indicator confirm password input and role selector showing two big toggle cards أنا لاعب for customer and عندي ملعب for owner each with an icon. Register button calls POST /api/auth/register and on success saves token and user and redirects based on role. The areas dropdown must use these real Amman areas based on the actual Amman map organized by direction from city center: Central Amman areas are وسط البلد and جبل عمان and الشميساني and الرابية and عبدون and دوار الداخلية. West Amman areas are خلدا and دابوق and الرونق and تلاع العلي and مرج الحمام and النزهة and الصويفية and دير غبار and أم أذينة and ام السماق and الجبيهة and الجاردنز. East Amman areas are الوحدات and ماركا and المقابلين and الهاشمي and الزرقاء Road and أبو نصير and الرصيفة Road. North Amman areas are طارق and الجويدة and خريبة السوق and البنيات and ناعور Road. South Amman areas are الحي الجنوبي and القويسمة and الجاهلية and الموقر Road. The design system is: page background #0a0f1e, panel #111827, card #1a2236, border #1e2d45, primary green #00c853 for available slots and CTAs and success, accent gold #f0c040 for pricing and ratings and highlights, open match blue #2196f3 for open match badges and slots with pulsing animation, booked red #f44336 for booked slots and errors, text primary #f0f0f0, text muted #8899aa, Arabic font Tajawal from Google Fonts, English and numbers font Inter from Google Fonts, border radius 12px for cards 8px for inputs 20px for badges, shadow 0 4px 24px rgba(0,0,0,0.4), navbar height 64px fixed top, sidebar width 240px fixed right on dashboard, transition all 0.2s ease. Slot colors are: available slots have background #00c853 and black text and pointer cursor. Booked slots have background #1a2236 and red #f44336 text and red border and not-allowed cursor and line-through text. Open match slots have background #1a2236 and blue #2196f3 text and blue border and a pulsing box-shadow animation keyframe going from 0 shadow to 0 0 0 8px transparent at 50% using rgba(33,150,243,0.5) and pointer cursor. Open match court card badge pulses the same way. All JS files are separate and loaded via script tags in this order: settings.js first then auth.js then api.js then the page-specific JS last. All CSS files are separate linked in head. No inline styles except dynamic JS color changes. All API calls go through api.js functions only never call fetch directly from page JS files. RTL layout using dir=rtl on html tag and direction rtl in CSS. Numbers and prices always LTR inside RTL context using a span with dir=ltr wrapper. No alert no prompt no window.confirm anywhere. Toasts appear bottom-left for RTL slide in from left auto-dismiss after 3 seconds max 3 visible color coded green for success red for error orange for warning each with an X to manually dismiss. All modals one at a time ESC closes backdrop click closes no duplicate listeners. All forms inline validation required fields get red border on failed submit. Loading skeletons on all data fetches. Fully responsive 360px to 1920px. Smooth scroll behavior. All images lazy loaded. The C# models for my backend are: Court with int Id string Name string Area string City string Type as indoor or outdoor string Surface double Rating int ReviewCount List of string Images List of string Amenities List of PricingTier Pricing string Sport and int OwnerId. PricingTier with int Duration in minutes string Label and decimal Price in JOD. Slot with int Id int CourtId string Date as YYYY-MM-DD string Time as HH:mm string Status as available or booked or open_match nullable int PlayersNeeded bool IsRecurring and List of int RecurringDays. Booking with int Id int CourtId int SlotId int CustomerId string Date int Duration decimal Amount string Status as pending or confirmed or cancelled and bool OpenForJoin. OpenMatch with int Id int BookingId int CourtId string Date string Time int PlayersNeeded int PlayersJoined and List of int JoinedUserIds. AppUser with int Id string Name string Email string Phone string Role as customer or owner and string PasswordHash. Enable CORS in Program.cs with AllowAnyOrigin AllowAnyMethod AllowAnyHeader. Deliver every file completely with zero truncation in this exact order. For each file write === FILENAME START === then the complete file content then === FILENAME END ===. Deliver in this order: settings.json then css/main.css then css/court.css then css/dashboard.css then js/settings.js then js/auth.js then js/api.js then js/index.js then js/court.js then js/matches.js then js/dashboard.js then index.html then court.html then matches.html then dashboard.html then login.html. After all 16 files write: DELIVERY COMPLETE — all 16 files fully functional zero truncation zero placeholders. Do not stop early. Do not truncate any file. Do not write same as before or rest of code here or any placeholder comment. Every single line of every file must be present. If the response is long keep writing until all 16 END markers are written.